module uczenie


//printfn "Hello from F#"

//let x = 5;
//let y = "ala"

//let mutable a = "dobry";

//printfn "Wartosc liczby x = %d" x;
//printfn "Dzien %s" a;


//printfn "Podaj imie";
//let name = System.Console.ReadLine();

//printfn "Podales imie: %s" name

//printf "Podaj liczbe:"

//let number = System.Console.ReadLine();
//let mutable nr = int number;

//printf "Podales: %i" nr;

//while nr > 2 do
//    printf "%i " nr
//    nr <- nr - 1;

//let listaA = [1;2;3];
//let listaB = 0 :: listaA;

//for x in listaB do
//    printf "%d" x

//let listaC = listaA @ listaB;

//for x in listaC do
//    printfn "%d" x

//printfn "%A" listaC

(*type Osoba = {
    imie: string;
    nazwisko: string;
    wiek: int;
}

let osoba1 = {imie = "ukasz"; nazwisko = "fryc"; wiek = 12};
let osoba2 = {imie = "ukasz"; nazwisko = "fryc"; wiek = 21};

printfn "%b" (osoba1 = osoba2)

let krotka = (1, "dwa", false);*)
